'use strict';

const app = document.getElementById('app');
const table = createHtmlElement(app, '', 'table', ['main-table']);
const fragment = document.createDocumentFragment();

window.addEventListener('DOMContentLoaded', initApp);
